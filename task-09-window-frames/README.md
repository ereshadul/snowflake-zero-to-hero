# Task 9 — Window frame clauses (ROWS/RANGE BETWEEN)

**Category:** Advanced SQL

## Goal
Cumulative sums and moving averages by controlling exactly which rows a window function sees.

## Steps
1. Run `10_window_frames.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `10_window_frames.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
