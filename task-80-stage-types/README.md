# Task 80 — Internal stage types

**Category:** Stages & File Management

## Goal
The three kinds of internal stage -- user (@~), table (@%table), and named -- and when project work actually calls for each.

## Steps
1. Run `81_stage_types.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `81_stage_types.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
