# Task 22 — Reading EXPLAIN and query plans

**Category:** Advanced Snowflake SQL

## Goal
Reading a query plan before it runs to predict where it'll actually spend time, as a complement to the post-run Query Profile task.

## Steps
1. Run `23_explain_query_plans.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `23_explain_query_plans.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
