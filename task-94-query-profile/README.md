# Task 94 — Query profile

**Category:** Performance & cost

## Goal
Reading the query profile graph to find the actual bottleneck in a slow query.

## Steps
1. Run `95_query_profile.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `95_query_profile.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
