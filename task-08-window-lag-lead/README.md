# Task 8 — Window functions: LAG and LEAD

**Category:** Advanced SQL

## Goal
Comparing a row to the row before/after it within a partition -- period-over-period deltas without a self-join.

## Steps
1. Run `09_window_lag_lead.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `09_window_lag_lead.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
