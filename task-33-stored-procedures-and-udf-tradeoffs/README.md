# Task 33 — Stored procedures vs. UDFs

**Category:** Programmability

## Goal
Why TRUNCATE (or any DDL/DML) requires a stored procedure, not a UDF, and when to reach for each.

## Steps
1. Run `34_stored_procedures_and_udf_tradeoffs.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `34_stored_procedures_and_udf_tradeoffs.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
