# Task 14 — CASE expressions and conditional aggregation

**Category:** Advanced SQL

## Goal
Turning row-level conditions into pivoted aggregate columns with CASE inside SUM/COUNT.

## Steps
1. Run `15_case_conditional_aggregation.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `15_case_conditional_aggregation.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
