# Task 109 — dbt incremental models

**Category:** dbt

## Goal
Materializing a model so it only processes new/changed rows on subsequent runs instead of rebuilding the whole table every time.

## Steps
1. Run `110_dbt_incremental_models.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `110_dbt_incremental_models.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
