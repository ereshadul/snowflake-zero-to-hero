# Task 58 — Zero-copy clone (schema/database)

**Category:** Time Travel & cloning

## Goal
Cloning at the schema/database level, e.g. for a full env snapshot.

## Steps
1. Run `59_zero_copy_clone_schema_db.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `59_zero_copy_clone_schema_db.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
