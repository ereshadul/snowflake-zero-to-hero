# Task 110 — dbt snapshots (SCD Type 2)

**Category:** dbt

## Goal
Capturing how a mutable source table's rows change over time using dbt's built-in Type 2 slowly-changing-dimension pattern.

## Steps
1. Run `111_dbt_snapshots.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `111_dbt_snapshots.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
