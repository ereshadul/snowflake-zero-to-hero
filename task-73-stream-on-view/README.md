# Task 73 — Stream on a view

**Category:** Streams

## Goal
Change tracking on a view instead of a base table.

## Steps
1. Run `74_stream_on_view.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `74_stream_on_view.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
