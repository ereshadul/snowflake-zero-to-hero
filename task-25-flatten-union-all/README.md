# Task 25 — FLATTEN + UNION ALL multiple records

**Category:** Semi-structured data

## Goal
Flattening many JSON documents at once and stitching the results together.

## Steps
1. Run `26_flatten_union_all.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `26_flatten_union_all.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
