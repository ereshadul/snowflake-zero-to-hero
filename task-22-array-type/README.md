# Task 22 — ARRAY data type

**Category:** Semi-structured data

## Goal
Storing and querying ordered lists natively with ARRAY.

## Steps
1. Run `23_array_type.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `23_array_type.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
