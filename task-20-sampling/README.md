# Task 20 — Sampling (SAMPLE / TABLESAMPLE)

**Category:** Advanced Snowflake SQL

## Goal
Querying a random subset of a huge table cheaply, and the difference between ROW and BERNOULLI/SYSTEM sampling.

## Steps
1. Run `21_sampling.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `21_sampling.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
