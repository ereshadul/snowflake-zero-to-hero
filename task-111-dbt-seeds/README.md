# Task 111 — dbt seeds

**Category:** dbt

## Goal
Version-controlling small static reference/lookup data as CSV files that dbt loads directly into tables.

## Steps
1. Run `112_dbt_seeds.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `112_dbt_seeds.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
