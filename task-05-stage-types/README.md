# Task 5 — Internal stage types

**Category:** Stages & File Management

## Goal
The three kinds of internal stage -- user (@~), table (@%table), and named -- and when project work actually calls for each.

## Steps
1. Requires Task 1 already done — `RAW.IOT_STAGE` needs to exist.
2. Run `06_stage_types.sql`. No file uploads needed — this task is
   purely about comparing the three stage types your account already
   has, with zero setup.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `06_stage_types.sql` — three questions on grants,
what happens to each stage type when its owning table/schema is
dropped, and file format defaults.
