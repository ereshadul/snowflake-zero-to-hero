# Task 55 — AT / BEFORE queries

**Category:** Time Travel & cloning

## Goal
Querying a table as it existed at a past timestamp, offset, or query ID.

## Steps
1. Run `56_time_travel_at_before.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `56_time_travel_at_before.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
