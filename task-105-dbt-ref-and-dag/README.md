# Task 105 — dbt ref() and building a model DAG

**Category:** dbt

## Goal
Using ref() instead of hardcoded table names so dbt can infer dependency order automatically, and reading the resulting DAG.

## Steps
1. Run `106_dbt_ref_and_dag.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `106_dbt_ref_and_dag.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
