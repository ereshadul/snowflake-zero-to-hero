# Task 108 — dbt macros and Jinja templating

**Category:** dbt

## Goal
Writing a reusable macro with Jinja to stop copy-pasting the same SQL pattern across models.

## Steps
1. Run `109_dbt_macros_jinja.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `109_dbt_macros_jinja.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
