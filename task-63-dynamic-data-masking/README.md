# Task 63 — Dynamic data masking

**Category:** Security

## Goal
Masking column values for some roles but not others without duplicating data.

## Steps
1. Run `64_dynamic_data_masking.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `64_dynamic_data_masking.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
