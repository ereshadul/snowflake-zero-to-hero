# Task 63 — Comparing two tables for drift

**Category:** Data Integrity & Quality

## Goal
Comparing two tables (e.g. QA vs. prod) for drift with MINUS/EXCEPT and hash-based row comparison.

## Steps
1. Run `64_table_diffing.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `64_table_diffing.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
