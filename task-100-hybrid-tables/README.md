# Task 100 — Hybrid Tables

**Category:** Newer table types

## Goal
Row-oriented, OLTP-style tables with enforced primary keys and fast point lookups.

## Steps
1. Run `101_hybrid_tables.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `101_hybrid_tables.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
