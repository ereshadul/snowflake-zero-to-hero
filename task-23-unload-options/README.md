# Task 23 — Unload options (SINGLE/MAX_FILE_SIZE/HEADER/OVERWRITE)

**Category:** Unloading data

## Goal
Controlling file splitting, headers, and overwrite behavior on unload.

## Steps
1. Run `24_unload_options.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `24_unload_options.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
