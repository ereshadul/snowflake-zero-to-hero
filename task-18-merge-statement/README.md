# Task 18 — MERGE statement deep dive

**Category:** Advanced Snowflake SQL

## Goal
Upsert logic in one statement -- WHEN MATCHED/NOT MATCHED, and the gotchas around matching more than one row.

## Steps
1. Run `19_merge_statement.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `19_merge_statement.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
