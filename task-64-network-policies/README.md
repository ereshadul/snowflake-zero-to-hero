# Task 64 — Network policies (conceptual)

**Category:** Security

## Goal
IP allow/block lists at the account or user level (conceptual, since this lab has no VPC).

## Steps
1. Run `65_network_policies.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `65_network_policies.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
