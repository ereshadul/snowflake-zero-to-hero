# Task 12 — ON_ERROR = SKIP_FILE_<percent>%

**Category:** Loading & COPY options

## Goal
Tolerating a percentage of bad rows per file instead of a fixed count.

## Steps
1. Run `13_on_error_skip_file_percent.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `13_on_error_skip_file_percent.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
