# Task 37 — Change-tracking columns (METADATA$...)

**Category:** Streams

## Goal
Reading METADATA$ACTION/ISUPDATE/ROW_ID to interpret stream output correctly.

## Steps
1. Run `38_change_tracking_columns.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `38_change_tracking_columns.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
