# Task 33 — Stream + Task CDC pattern

**Category:** Streams

## Goal
Wiring a stream and a task together into a self-driving CDC pipeline.

## Steps
1. Run `34_stream_task_cdc.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `34_stream_task_cdc.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
