# Task 5 — AWS S3 + IAM setup for Snowflake

**Category:** AWS Integration

## Goal
Creating an S3 bucket and the IAM policy/role Snowflake needs to read from it -- the prerequisite plumbing before any STORAGE INTEGRATION can exist.

## Steps
1. Run `06_s3_iam_setup.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `06_s3_iam_setup.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
