# Task 21 — LATERAL joins with table functions

**Category:** Advanced Snowflake SQL

## Goal
Calling a table function once per row of an outer query -- the general mechanism behind FLATTEN.

## Steps
1. Run `22_lateral_table_functions.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `22_lateral_table_functions.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
