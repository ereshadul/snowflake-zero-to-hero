# Task 110 — Semantic layer & "Talk to Data"

**Category:** Ecosystem & Modeling

## Goal
Building a semantic model so Cortex Analyst can answer
natural-language questions over your data directly — the layer that
turns the star schema from Task 105 into something a non-SQL user can
query in plain English.

## Steps
1. Run `111_semantic_layer_talk_to_data.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `111_semantic_layer_talk_to_data.sql`. Answer by
actually running the diagnostic queries it points to, not from
memory.

*(Status: scaffolded — SQL content not yet written.)*
