# Task 106 — dbt schema tests (unique, not_null, relationships, accepted_values)

**Category:** dbt

## Goal
The four built-in generic tests declared in YAML, and what each one actually catches.

## Steps
1. Run `107_dbt_schema_tests.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `107_dbt_schema_tests.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
