# Task 79 — PERMANENT vs. TRANSIENT vs. TEMPORARY tables

**Category:** Table Types

## Goal
Time Travel/Fail-safe differences between the three table types, why daily-truncated staging tables usually don't need PERMANENT, and whether a permanent table can live inside a transient schema.

## Steps
1. Run `80_transient_temporary_permanent_tables.sql`.
2. Work through it section by section against your own Snowflake trial
   — don't just read it.
3. Answer the understanding check below before moving to the next task.

## Understanding check
See the bottom of `80_transient_temporary_permanent_tables.sql`. Answer by actually running the
diagnostic queries it points to, not from memory.

*(Status: scaffolded — SQL content not yet written.)*
